# 🤖 AI 多模型协作开发规范（强制）

> 适用于本项目，多 AI 协作时必须严格遵守。

## 分工原则

| 角色 | 模型 | 职责 |
|------|------|------|
| 计划者 | Claude Opus 4.6 | 架构设计、任务拆解、代码审计 |
| 执行者 | GPT-5.5 high | 代码编写、非核心逻辑实现 |
| 协作者 | Codex（MCP） | 代码补全、实时协作 |

## 10 条核心规范

1. **文档先行**：每个功能必须先写开发文档、接口文档、TODO，再开始编码。
2. **计划与执行分工**：Claude Opus 4.6 做计划，GPT-5.5 high 做执行。
3. **单一职责**：所有文件必须单一职责，严禁一个文件混合多种业务逻辑（如既处理登录又处理查询）。
4. **Handoff 文档**：每次新建对话前必须写 handoff 文档，总结本次做了什么、有什么变更、还有什么未完成。
5. **测试同步**：功能测试必须与功能代码一起写，所有测试全部跑通才允许合并。
6. **核心业务手写**：核心业务逻辑必须自己手写，只让 AI 写非核心部分。
7. **全局规则锁定**：建好全局规则锁定技术栈版本、包管理器和代码风格，不得随意变更。
8. **类型优先**：先定接口定义和数据结构，再写业务逻辑。
9. **单步指令**：每次只下达一步指令，绝不允许 AI 一口气把前后端全写完。
10. **定期审计**：每完成几个模块，让 Claude 做代码审计，清理冗余代码并排查越权访问等安全漏洞。

**优先级：🔴 最高**
